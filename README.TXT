ThymeLeaf version

If you want to run it "local" in IDE use NO application name : http://localhost:8080/
If you want to run it "local" in IDE using application name : UNCOMMENT name in application properties

To run on CLOUD COMMENT out the name in application Properties 